apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: ticket-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: nginx
        image: shinojl4dx/frontend@sha256:de258aa504ec37610db12de9eaabf8c5e8347c0f342920e17c6025b0c8f733e9
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: ticket-system
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80        # cluster port
      targetPort: 80  # container port
      nodePort: 30080 # p
